machine:
    timezone:
        America/Chicago
    php:
        version:
            7.2.0

dependencies:
    override:
        - php --version
        - composer install --prefer-source --no-interaction
    cache_directories:
        - ~/.composer/cache

    pre:
        - sudo composer self-update
test:
    post:
        - phpunit